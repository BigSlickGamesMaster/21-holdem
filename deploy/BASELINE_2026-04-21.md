# Baseline Checkpoint - 2026-04-21

This checkpoint captures the current working `21 Hold'em` prod-like local stack state.

## Active Stack

- Player frontend: `http://192.168.0.205:3101`
- Admin frontend: `http://192.168.0.205:3102`
- Game backend: `http://192.168.0.205:4100`
- Admin backend: `http://192.168.0.205:4151`
- MongoDB: `192.168.0.205:27019`
- Redis: `192.168.0.205:6382`

## Current DB Snapshot

- Mongo database: `holdem`
- Snapshot folder: `deploy/backups/prod-like-baseline-2026-04-21`
- Expected state at checkpoint time:
  - `board_prototypes`: 2
  - `users`: 22

## Known Working Credentials

- Admin portal:
  - email: `local-admin@example.com`
  - password: `Admin123!`

- Game login:
  - username: `Br3nt`
  - password: `Game123!`

## Start Stack

```powershell
Set-Location D:\BIGSLICKGAMES\games\Bigslickgames
docker compose -f .\deploy\docker-compose.prod-like.yml up -d --build
```

## Stop Stack

```powershell
Set-Location D:\BIGSLICKGAMES\games\Bigslickgames
docker compose -f .\deploy\docker-compose.prod-like.yml down
```

## Restore Mongo Snapshot

```powershell
Set-Location D:\BIGSLICKGAMES\games\Bigslickgames
docker compose -f .\deploy\docker-compose.prod-like.yml up -d mongo
docker cp .\deploy\backups\prod-like-baseline-2026-04-21\holdem.archive.gz holdem-mongo-prod-like:/tmp/holdem.archive.gz
docker exec holdem-mongo-prod-like mongorestore --gzip --archive=/tmp/holdem.archive.gz --drop
```

## Notes

- This is a local prod-like checkpoint, not a real EC2 deployment snapshot.
- The admin frontend includes the max-player field fix.
- The admin backend and game backend are both pointed at the same prod-like Mongo container.
- `Admin-Backend/index.js` still needs security review before any real deployment.
